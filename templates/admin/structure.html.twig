{% extends 'admin/base.html.twig' %}
{% block title %}<title>structure</title>{% endblock title %}
   {% block css %} <link rel="stylesheet" href="{{ asset ('assets/css/style.css') }}"/>{% endblock css %}



{% block flash %}
{% for message in app.flashes('success') %}
{{message}}
{% endfor %}
{% endblock flash %}

   {% block body %}
 
    {% for structures in structures %}
        
    
    <h1>{{ structure.name }}</h1>  
    <h2>{{ structure.description }}</h2>
        
       
          
        <button type="submit" name"modifier_structure">Modifier structure</button>
        <button type="submit" name"activer_structure">Activier structure</button>
           
    
    {% endfor %}
    {{ dump(structure)}}
    {% endblock body %}
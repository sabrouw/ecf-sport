{% extends 'base.html.twig' %}
{% block title %}<title>Authentification</title>{% endblock title %}
{% block body %}




{% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

      
<div class="container mt-5 containLogForm">
    
   <form class="form-control mt-4 " id="formLogin" action="{{ path ('login') }}" method="post">
    
    <h1 class="login-h1 mt-4 "> Merci de vous authentifier </h1>
    
        <label class="text-bold form-label " for="username"id="label_username">Email</label>
        <input class="form-control mt-4" type="text" id="username" name="_username" value="{{ last_username }}"/>
    
        <label class="text-bold form-label mt-4" for="password" id="label_password" >Mot de passe</label>
        <input class="form-control"type="password" id="password" name="_password"/>
    
        {# If you want to control the URL the user is redirected to on success
        <input type="hidden" name="_target_path" value="/"/>#}

        <button id="buttauth"class="btn btn-dark mt-4" type="submit">Connection</button>
        <a id="buttpass" class="btn btn-dark mt-4"href="{{ path ('passe_oublie') }}"> Mot de passe oublié </a>
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
    </form>
</div>


{% endblock %}
{% block javascript %}{% endblock javascript %}